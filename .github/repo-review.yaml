repo_slug: browser-extensions
repository: acate-suki/browser-extensions
description: Chrome extensions package review.
jobs:
  - id: unit-test
    name: Format, lint, typecheck, coverage
    target: unit-test
  - id: build
    name: Production bundle builds
    target: build
